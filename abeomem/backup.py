"""Stub — full implementation lives in T6.4."""

from __future__ import annotations

from pathlib import Path


def run_backup(db_path: Path, cfg, explicit_target: Path | None = None) -> Path | None:
    """Placeholder; T6.4 implements checkpoint + VACUUM INTO + rotation."""
    return None
